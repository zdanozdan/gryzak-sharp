<Window x:Class="Gryzak.Views.GlsParcelSearchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="🧪 Szukaj przesyłki GLS" 
        Height="560" Width="640"
        MinHeight="470" MinWidth="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Icon="pack://application:,,,/gryzak_icon.png">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Wyszukiwanie po numerze paczki GLS (tracking), np. z weba „Paczki → Numer”"
                       FontSize="12" Foreground="Gray" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Numer paczki:" VerticalAlignment="Center" Width="120"/>
            <TextBox Name="ParcelNumberTextBox" Width="320" MinWidth="240" Padding="8" />
            <Button Name="SearchButton"
                    Content="Szukaj"
                    Margin="10,0,0,0"
                    Padding="12,8"
                    MinWidth="100"
                    Click="SearchButton_Click"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1" Padding="10" Background="#F9F9F9">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Name="StatusTextBlock" Grid.Row="0" FontWeight="SemiBold" Margin="0,0,0,10" />
                <TextBox Name="ResultTextBox"
                         Grid.Row="1"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         FontFamily="Consolas"
                         FontSize="12"
                         />
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Zamknij" Click="CloseButton_Click" IsCancel="True" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>

