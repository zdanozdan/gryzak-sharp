<Window x:Class="Gryzak.Views.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Debug - Logi aplikacji"
        Height="600" Width="800"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        ShowInTaskbar="True"
        Topmost="False"
        Icon="pack://application:,,,/gryzak_icon.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Nagłówek -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Image Grid.Column="0" 
                       Source="pack://application:,,,/gryzak.png" 
                       Width="32" 
                       Height="32" 
                       Margin="0,0,15,0" 
                       VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Okno debugowania" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="White"
                               Margin="0,0,0,3"/>
                    <TextBlock Text="Logi z Console.WriteLine" 
                               FontSize="12" 
                               Foreground="#E3F2FD"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <CheckBox Name="LogToFileCheckBox"
                              Content="Loguj do pliku"
                              VerticalAlignment="Center"
                              Margin="0,0,15,0"
                              Checked="LogToFileCheckBox_Checked"
                              Unchecked="LogToFileCheckBox_Unchecked"
                              Foreground="White"
                              FontSize="12"/>
                    <Button Name="ClearButton" 
                            Content="Wyczyść" 
                            Width="80" 
                            Height="30" 
                            Margin="0,0,10,0"
                            Click="ClearButton_Click"
                            Background="White"
                            Foreground="#2196F3"
                            BorderThickness="0"
                            Cursor="Hand"/>
                    <Button Name="CloseButton" 
                            Content="Zamknij" 
                            Width="80" 
                            Height="30"
                            Click="CloseButton_Click"
                            Background="White"
                            Foreground="#2196F3"
                            BorderThickness="0"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Zawartość logów -->
        <Border Grid.Row="1" Margin="10" Background="#1E1E1E" BorderThickness="1" BorderBrush="#333333">
            <TextBox Name="LogTextBox" 
                     IsReadOnly="True"
                     TextWrapping="NoWrap"
                     FontFamily="Consolas, Courier New"
                     FontSize="11"
                     Background="#1E1E1E"
                     Foreground="#D4D4D4"
                     BorderThickness="0"
                     Padding="10"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"/>
        </Border>
        
        <!-- Status bar -->
        <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Name="StatusTextBlock" 
                           Text="Gotowy do logowania..."
                           FontSize="11"
                           Foreground="#666666"/>
                <TextBlock Name="LogFileStatusTextBlock"
                           Grid.Column="1"
                           Text=""
                           FontSize="11"
                           Foreground="#2196F3"
                           Margin="10,0,0,0"
                           Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</Window>

